language: python

python:
  - 3.6

before_install:
  - python --version
  - pip --version
  - uname -a
  - lsb_release -a

install:
  - pip install -r requirements.txt
  - python setup.py install

script:
  - make pep8
  - py.test
